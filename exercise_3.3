evenNumbers = []
oddNumbers = []

choice = "i"

while (choice != "q"):
    if choice == "i":
        number =int(input("Give me a number: "))
        if number%2==0:
            evenNumbers.append(number)
            evenNumbers.sort()
            evenNumbers.reverse()
            print("The ordered list is: ",evenNumbers)
        elif number%2!=0:
            oddNumbers.append(number)
            oddNumbers.sort()
            oddNumbers.reverse()
            print("The ordered list is: ",oddNumbers)
            
    elif choice == "d":
        number =int(input("Give me a number: "))
        if number%2==0:
            for idex,num in enumerate(evenNumbers):
                if number==num:
                    evenNumbers.pop(index)
                print("The ordered list is: ",evenNumbers)
        elif number%2!=0:
            for index,num in enumerate(oddNumbers):
                if number==num:
                    oddNumbers.pop(index)
                print("The ordered list is: ",oddNumbers)
                
    elif choice == "q":
        break
    choice=input("Press i to insert new value, d to delete a value or q to exit: ")
print("Thank you")
                
    
